{
  "name": "team-management-server",
  "version": "1.0.0",
  "description": "JSON Server for Team Management System",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001 --routes routes.json",
    "dev": "json-server --watch db.json --port 3001 --routes routes.json --delay 200"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
